# Mia for VP campaign

See [`AGENTS.md`](AGENTS.md) for the rules that bind every agent in this repo — they apply here in
full. The two that matter most:

- **Read [`brand/DESIGN.md`](brand/DESIGN.md) before writing any UI**, and reference `--mv-*` tokens
  for every color, font, size, radius and duration. Run the build-check, lint and contrast commands
  in `AGENTS.md` before calling UI work done. `brand/preview/index.html` is the visual test.
- **Read [`PROJECT_TRACKER.md`](PROJECT_TRACKER.md) first** (a symlink into the memory vault) and
  keep it current as part of "done" — status board, evidence with numbers, traps, decisions.

Never invent campaign facts: the election date, platform points, quotes, and Mia's surname are
placeholders until supplied.
