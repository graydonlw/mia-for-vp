<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" role="img" aria-label="Mia for VP">
  <!-- Colors are brand tokens: rose (--mv-primary) on white (--mv-on-primary).
       An icon file cannot read CSS custom properties; keep these in sync with
       brand/tokens/tokens.json if the palette ever changes. -->
  <rect width="32" height="32" rx="16" fill="#BE4C78"/>
  <text x="16" y="22" text-anchor="middle" fill="#FFFFFF"
        font-family="Fredoka, 'Trebuchet MS', Verdana, sans-serif" font-size="16" font-weight="600">M</text>
</svg>
